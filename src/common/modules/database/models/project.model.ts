export enum ProjectStatus {
  InProgress = 'in progress',
  Completed = 'completed',
  OnHold = 'on hold',
  Cancelled = 'cancelled',
}

export enum ProjectPriority {
  Low = 'low',
  Medium = 'medium',
  High = 'high',
}
